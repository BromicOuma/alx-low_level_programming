#include <stdlib.h>
#include "main.h"

/**
 *  _abs - function to compute absolute value
 *@n: takes an integer variable
 *Return: returns the absolute value of a number
 */
int _abs(int n)
{
	n = abs(n);
	return (n);
}
