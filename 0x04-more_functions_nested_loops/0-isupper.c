#include <main.h>

/**
 * _isupper - check if a letter is uppercase
 * @c: the character
 *
 * Return: the function returns 1 when character is 
 */

int _isupper(int c)
{
	if (c => 65 && c <= 98)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
