#incude "main.h"

/**
 * _isdigit - input digit i between 0 to 9
 *
 * @c: input value
 * Return: Return 1 if c is digit and 0 if not
 */

int _isdigit(int c)
{
	if (isdigit(c))
	{
	return (1);
	}
	return (0);
}
