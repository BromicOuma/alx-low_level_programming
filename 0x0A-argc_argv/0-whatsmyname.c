#include "main.h"

/**
 * main - function takes command line arguments as input
 *
 * @argc: count of arguments
 * @argv: pointer to array of characters
 *
 * Return: returns success on successful compilation.
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
